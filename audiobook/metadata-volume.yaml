apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metadata
  namespace: audiobook
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
