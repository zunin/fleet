apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: audiobook
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
