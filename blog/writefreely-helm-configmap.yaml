apiVersion: v1
kind: ConfigMap
metadata:
  name: writefreely-helm-values
  namespace: blog
data:
  values.yaml: |
    image:
      repository: docker.io/algernon/writefreely
      tag: "0.13.2-2"
    ingress:
      enabled: true
      hosts:
        - host: blog.nikolaiathome.com
          paths:
            - pathType: Prefix
              path: /
