apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music-volume
  namespace: music
  labels:
    app.kubernetes.io/component: volume
    app.kubernetes.io/app: music
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 150Gi
