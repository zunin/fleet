apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-data
  namespace: health
  labels:
    name: gatus-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi

