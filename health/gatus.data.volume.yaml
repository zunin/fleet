apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-data
  namespace: health
  labels:
    app.kubernetes.io/component: volume
    app.kubernetes.io/app: gatus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi

