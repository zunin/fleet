apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: contour
  namespace: projectcontour
spec:
  interval: 1m
  chart:
    spec:
      chart: contour
      # renovate: datasource=helm depName=contour/contour
      version: "0.4.0"
      sourceRef:
        kind: HelmRepository
        name: contour
        namespace: projectcontour
      interval: 1m

